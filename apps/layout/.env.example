# cp .env.example .env

DOMAIN=layout.dae-da.com

# 로컬: API dev 서버 (layout 저장/불러오기)
VITE_API_BASE=http://localhost:4000

# wedding_rsvp/deploy docker compose network (deploy/ 에서 ./deploy.sh 실행 시)
DOCKER_NETWORK=deploy_default

# standalone Caddy 모드에서만 사용
ACME_EMAIL=
