/** Pixel size of one layout grid cell (Kenney tile = 16px). */
export const CELL_PX = 16

/** Legacy fine grid — loaded layouts with this cellPx are downgraded to {@link CELL_PX}. */
export const FINE_CELL_PX = 8

export const DEFAULT_GRID_WIDTH = 128
export const DEFAULT_GRID_HEIGHT = 96

export const VENUE_GRID_WIDTH = 120
export const VENUE_GRID_HEIGHT = 85
