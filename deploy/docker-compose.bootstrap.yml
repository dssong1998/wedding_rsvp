services:
  nginx:
    ports:
      - "80:80"
    volumes:
      - ./nginx/bootstrap-http.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
